version: "3.9"
services:
  web:
    image: php:8.1-fpm-alpine
  cache:
    image: redis
  database:
    image: mariadb:focal
  broadcast:
    image: quay.io/soketi/soketi:1.0-16-alpine
  filesystem:
    image: minio/minio:latest
  queue:
    image: redis
  session:
    image: redis
volumes:
  logvolume01: {}
